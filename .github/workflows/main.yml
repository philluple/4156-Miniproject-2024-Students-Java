name: Build CI

on: 
  push:
    branches: [main]
  pull_request: 
    branches: [main]

jobs: 
  build: 
    runs-on: mac-latest
    steps: 
    - name: Checkout code
      uses: actions/checkout@v2
